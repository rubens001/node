{
  "name": "typesrc",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index",
  "typings": "lib/index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build:live",
    "build:live": "nodemon --exec ./node_modules/.bin/ts-node -- ./index.ts"
  },
  "author": "Rubens Zanatta",
  "license": "ISC",
  "engines": {
    "node": "~5.6"
  },
  "devDependencies": {
    "@types/node": "^7.0.12",
    "nodemon": "^1.11.0",
    "ts-node": "^3.0.2",
    "typescript": "^2.2.2"
  },
  "dependencies": {
    "body-parser": "^1.17.1",
    "express": "^4.15.2"
  }
}
